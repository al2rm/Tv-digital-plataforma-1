# Actualización V2

## Backend
1. Copia la carpeta `backend` sobre tu backend actual.
2. Edita `backend/src/app.js` siguiendo `backend/INTEGRAR-EN-APP.txt`.
3. Ejecuta `npm run db:migrate`.

## Panel
1. Copia la carpeta `admin` sobre tu panel actual.
2. Copia el contenido de `admin/styles-v2.css` al final de `admin/src/styles/global.css`.

## Commit sugerido
`Agrega gestión administrativa v2`
